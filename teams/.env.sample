# Teams API Configuration
# Auth0 Teams API uses opaque access tokens (not JWT M2M tokens)

# Your team slug (used to construct https://{TEAM_SLUG}.teams.auth0.com)
TEAM_SLUG=your-team-slug

# Opaque access token for Teams API authentication
# Note: This is NOT a JWT token - it's an opaque token specific to Teams API
api_token=your_opaque_access_token

# Runtime environment (default: prod)
# Use 'prod' for teams.auth0.com
# Use 'sus' for teams.sus.auth0.com (or any other non-prod environment)
ENV=prod
